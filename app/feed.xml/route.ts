import { categoryPath, data, getItemProvince, sortItems } from "@/lib/data";
export const dynamic = "force-static";
function esc(v:string){return v.replace(/[<>&'"]/g,(c)=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c] as string));}
export function GET(){const site=process.env.NEXT_PUBLIC_SITE_URL??"https://sehirradar.example";const items=sortItems(data.items).slice(0,50).map(i=>`<item><title>${esc(`[${getItemProvince(i)}] ${i.title}`)}</title><link>${site}${categoryPath(i)}</link><guid>${site}${categoryPath(i)}</guid><description>${esc(i.summary)}</description><pubDate>${new Date(i.updatedAt).toUTCString()}</pubDate></item>`).join("");const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Şehir Radar Türkiye</title><link>${site}</link><description>81 il canlı şehir veri ağı</description><language>tr-TR</language>${items}</channel></rss>`;return new Response(xml,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, max-age=3600"}});}
